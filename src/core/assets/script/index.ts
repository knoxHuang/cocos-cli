
class ScriptManager {

    /**
     * TODO 查询脚本依赖关系
     * @param path 
     * @returns 
     */
    async queryScriptUser(path: string): Promise<string[]> {

        return [];
    }
}

export default new ScriptManager();
